# v0.3.2 attended release authority

## Authorization

David Ahmann authorized this release in the attended Codex conversation on
2026-09-09: “lets do new latest release incl npm and update docs to use new
version also reanalyze readme and agents.md and think of optimizations and
rewrites and address ship all till green”.

The authorization covers the exact `v0.3.2` patch release from the reviewed
current-main candidate: package and CLI versioning; the focused README, AGENTS,
release-runbook and distribution-documentation repairs; an annotated tag;
candidate and protected publish workflows; the preserved npm artifact; GitHub's
normal public-alpha release; npm `alpha` and `latest`; and provider readback.

It also authorizes the single exact GitHub `npm` environment tag rule for
`v0.3.2`, preserving the required reviewer and selected-tag policy. It does not
authorize a wildcard, bypass, weaker trusted publishing, modification of prior
tags/releases/packages, or a broader public support claim.

## Scope and stop conditions

The candidate changes only release identity, release documentation, the
repository operating contract, product-record continuity, and the integration
adaptation already merged on `main`. Historical tags, published package
versions, release records, and provider evidence remain immutable.

Stop before publication if the candidate tag, source tree, preserved artifact,
candidate run, qualification, npm provenance, registry readback, or GitHub asset
identity differs. A failed candidate or ambiguous external effect requires a new
reviewed version; it never permits retagging or republishing.
