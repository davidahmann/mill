# Mill v0.1.3 prepublication candidate

This immutable tag was not published. Its annotated message omitted the required
`Reviewed-Candidate-Tree` trailer, so both release builders rejected it at
identity verification before running the native gate or creating an artifact.
The corrected source continued under `v0.1.4`; the complete OIDC release
evidence continues under `v0.1.5`.

No npm package, GitHub Release, checksum, SBOM, or public support claim exists
for `v0.1.3`. The tag is retained as failure evidence and must not be moved or
reused.
