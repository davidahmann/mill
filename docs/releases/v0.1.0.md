# Mill v0.1.0 prepublication candidate

Status: withdrawn before publication

The immutable `v0.1.0` tag records a failed public-alpha candidate. Both clean
builders completed, but the qualification audit blocked before artifact
comparison because GitHub's checkout action writes the inert repository setting
`gc.auto=0`, which this candidate did not recognize.

No `@davidahmann/mill@0.1.0` package or GitHub Release was published. Do not use
this version. The narrow compatibility correction and complete restarted release
evidence belong to `v0.1.1`.
