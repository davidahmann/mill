# Mill v0.3.2

`0.3.2` is a public-alpha patch release that adds bounded integration-adaptation
evidence. An operator can bind a provider change, approved workflows and defined
configuration fixtures to a complete check-or-exclusion matrix. Candidate
evidence names each offline fixture result without claiming live-provider
execution, customer fidelity, or owner acceptance.

The release also makes baseline admission preserve command-verified affected
invariants, carries fixture expiry through attended merge effects, and permits a
dependency-free repository only when its npm lock proves an empty graph.

## Install

```sh
npm install --save-dev --ignore-scripts @davidahmann/mill@0.3.2
npx --no-install millctl --version
```

Pin the exact version. The npm `latest` channel is a distribution pointer, not
an automatic-update instruction; an existing repository must deliberately
requalify changed delivery machinery.

## Release evidence

The annotated tag is qualified through two independent builds, a preserved
tarball, packed greenfield/adoption canaries, the pinned independent policy,
protected npm OIDC publication, registry requalification, and GitHub asset
readback. The normal GitHub Release carries the tarball, checksum, SBOM, and
final evidence after those steps complete.

The public-alpha limits remain unchanged. This release does not add a supported
stack, grant autonomous merge or deployment authority, or establish customer or
live-provider compatibility.

## Completed publication readback

The immutable candidate run
[`34387631805`](https://github.com/davidahmann/mill/actions/runs/34387631805)
and protected publish run
[`34388616783`](https://github.com/davidahmann/mill/actions/runs/34388616783)
completed on 2026-09-09. The tag commit
`b48b9b167df6b57991e03075ba5c8237a3a8554c` and reviewed candidate tree
`c486696648e9927a206933507f4257ceefa0c529` match. Both independent builders and
the final GitHub asset readback identify the preserved tarball as
`sha256:cdb123740e57562c81f6557d1ba7dfac2e243616f6e639bac1620166955bdf15`.

The registry reports integrity
`sha512-bbcwN0geCWywa1lUDWLfftTigp9IyEGaMPYSoE2/lbFIXyr4vJ0CizNTJyBfU4+dytdB3eOxJiXUuV3HiY5HeQ==`;
the protected workflow verified npm provenance and signatures, then requalified
a fresh registry download. npm `alpha` and `latest` both resolve to `0.3.2`. The
`latest` promotion used the maintainer's attended security-key challenge and did
not rebuild or republish the artifact.

The public
[GitHub Release](https://github.com/davidahmann/mill/releases/tag/v0.3.2) is
normal, not a draft or prerelease, and carries the tarball, checksum, SBOM,
prepublication evidence, and final evidence. The qualified support tuple remains
`darwin-arm64-node24-codex`, exercised with Node 24.20.0 and npm 11.19.0; it
expires at `2026-10-05T15:35:52.039Z`. Its full identity is preserved in the
candidate qualification artifact.
