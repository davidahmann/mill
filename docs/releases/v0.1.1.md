# Mill v0.1.1 prepublication candidate

Status: withdrawn before publication

The immutable `v0.1.1` tag records a failed public-alpha candidate. Both clean
builders, exact-tag audit, and independent artifact comparison passed. The real
Linux packed-artifact canary then produced root-owned browser output through a
bind-mounted workspace, and the non-root runner could not remove it.

No `@davidahmann/mill@0.1.1` package or GitHub Release was published. Do not use
this version. The POSIX ownership correction moved to `v0.1.2`; the current
public-alpha candidate and restarted release evidence belong to `v0.1.4`.
